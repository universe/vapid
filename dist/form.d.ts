import { Json } from '@universe/util';
/**
 * Renders forms for the dashboard.
 */
export default class Form {
    static field(name: string, label: string, params: Record<string, string>, value: string, error: string, meta: Json): string;
}
