{
  "name": "@neutrinodev/runtime",
  "version": "1.0.0-beta.6",
  "license": "MIT",
  "description": "A template-driven content management system",
  "repository": "universe/vapid",
  "author": "Adam Miller <adam@universe.app>",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "type": "module",
  "scripts": {
    "lint": "eslint bin lib test",
    "test": "jest | pino-pretty",
    "emulate": "firebase emulators:start",
    "build": "rm -rf dist && tsc",
    "watch": "watch 'npm run build' './src' --wait=1"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "keywords": [
    "app",
    "cms",
    "framework",
    "web"
  ],
  "nodemonConfig": {
    "ignore": [
      "assets/*"
    ]
  },
  "files": [
    "assets",
    "bin",
    "dist"
  ],
  "dependencies": {
    "@glimmer/syntax": "0.83.1",
    "@neutrinodev/core": "1.0.0-beta.6",
    "@neutrinodev/stdlib": "1.0.0-beta.6",
    "@simple-dom/interface": "^1.4.0",
    "@simple-dom/serializer": "^1.4.0",
    "@universe/util": "^1.1.2"
  }
}
